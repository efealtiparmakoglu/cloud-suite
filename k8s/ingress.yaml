apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloud-suite-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: cloud-suite.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cloud-suite-service
            port:
              number: 80
